<nav class="navbar navbar-dark navbar-expand-lg bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Contacts Hub</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 w-100 d-flex justify-content-end">
        <li class="nav-item">
          <% if(user) { %>
            <a class="nav-link active" aria-current="page" href="/contact/index">Add Contacts</a>
            <% } %>
        </li>
        <li class="nav-item">
          <% if(user) { %>
            <a class="nav-link active" aria-current="page" href="/login/logout">Logout</a>
            <% } else { %>
              <a class="nav-link active" aria-current="page" href="/login/index">Login</a>
              <% } %>
        </li>
      </ul>
    </div>
  </div>
</nav>